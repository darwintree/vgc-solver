# Implementation Trace: 游戏见解与通用搜索优化

Date: 2026-09-08
Source: 用户要求同模型子代理在不同 worktree 探索可扩展优化，使当前所有 case 收敛。
Language: 中文

状态：用户已要求收尾并停止继续探索，最终交付见[实验记录](../../optimization-records/game-insight-search-2026-09-08.md)。以下条目和 Follow-up 保留当时决策，不作为继续执行的任务指令。

## Entries

### 1. 本轮验收集合

Type: interpretation

Context:
“当前所有 case”未重新指定集合与预算；当前讨论一直围绕 Champions 的 54 项受支持输入，其他 351 项此前允许跳过。

Decision:
主目标采用固定 Champions 快照的全部 54 项受支持输入，原生最大 PP、同步 workers=0、冷进程、搜索预算 10000 ms、容差 0.02；不重新抓取数据或改变输入。既有标准 fixture 与全套功能测试作为回归，分别报告，不能混称 405 项全部通过。最终目标项重复测量；探索期单样本只用于筛选。

Reason:
保持与最近 29/54 成绩相同的可比口径。扩大模拟器规则支持或把标准 fixture 的其他预算混入目标会改变当前任务。

Follow-up:
如用户明确扩大集合，再追加验收范围。

### 2. 并行方向与兼容性

Type: tradeoff

Context:
用户将 OHKO/2HKO hint 作为一个方向，并要求独立提出其他通用方向，未指定实现。

Decision:
从 51acee3 建立游戏见解提示、通用证明调度、转移成本三个隔离 worktree；同模型子代理实现，主代理比较与验收。已发布的 exact 完整矩阵、bounded 安全区间和 custom adapter 合同保留，内部调度可替换。暂不实施用户刚刚搁置的通用逐击恢复框架。

Reason:
将“少算哪些分支”与“每次计算更便宜”分别检验，避免同时改动掩盖收益来源。性能测量由主代理串行协调，与构建和功能测试分开。

Follow-up:
基于固定基线与候选实测迭代选择，不预承诺 hint 必然有效。

### 3. 不采用无明确收益的采样提示

Type: tradeoff

Context:
原生两回合重复动作采样能够生成提示，但在 joint 路径接入后，困难项实测仍弱于无提示渐进转移组合。游戏见解分析显示强化后攻击、Encore 后攻击等关键换招没有被重复动作表示。

Decision:
当前 `682e043` 主候选不携带采样提示，保留独立分支与消融结果。转向局部原生概率证书与实际执行热点分析；新策略提示只有在额外测量支持时重新考虑。

Reason:
“用了游戏见解”不是验收条件；安全性成立的启发式也可能浪费证明预算。保留固定目标与完整行动空间，避免为概念增加无收益的复杂度。

Follow-up:
首次全量 49/54 后仍需解决剩余项并完成重复验收。

### 4. 接入 PokaiTrainer 研究

Type: tradeoff

Context:
用户提供 `/tmp/pokemon-solver-pokaitrainer-handoff-2026-09-08.md` 并授权借鉴优化。其研究以 `51acee3` 为本地基线，部分转移安全界在当前候选已实现；成本感知排序也已进入独立实验。

Decision:
保留既有目标与实验，新增受限共享计算／HP 延迟展开方向。引用并保存已有研究，不重复撰写同一论文调查。先依据实际热点选择可审计的共享片段或延迟复制，在规则需要读取 HP 或下一决策边界保留具体联合状态，禁止用平均 HP 或相互独立的边缘分布替代它们。

Reason:
当前证据表明转移执行占主要成本，论文的共享计算思路有直接探索价值；其近似搜索默认阈值与本项目安全区间合同不同，不直接迁入。新的授权允许探索此方向，但并未要求无条件重写整个模拟器或引入大型回合中途恢复框架。

Follow-up:
各候选独立验证原生完整具体状态分布与限时收敛，不把论文单回合速度当成本项目收益。
